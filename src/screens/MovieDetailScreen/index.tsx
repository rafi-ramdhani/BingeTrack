import { AppText } from '@/components/AppText';
import { View } from 'react-native';

export const MovieDetailScreen = () => {
  return (
    <View>
      <AppText>Movie Detail Screen</AppText>
    </View>
  );
};
